import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "SOTA eKuiper Playground | Rule Engine IDE",
  description:
    "SOTA iiot eKuiper Rule Engine Playground - Build, test, and deploy multi-stage decision trees for IoT streaming data",
  keywords: ["eKuiper", "IoT", "Rule Engine", "MQTT", "Streaming", "SOTA"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
